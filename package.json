{
  "name": "nats-auth",
  "version": "1.0.0",
  "type": "module",
  "bin": "./dist/cli.js",
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf dist && yarn build",
    "cli": "tsx src/cli.ts",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "@types/node": "^20.14.0",
    "commander": "^15.0.0",
    "tsx": "^4.15.0",
    "zod": "^4.4.3"
  },
  "packageManager": "yarn@4.17.0",
  "devDependencies": {
    "@vitest/ui": "^4.1.9",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
