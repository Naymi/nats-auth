{
  "name": "nats-auth",
  "version": "1.0.0",
  "type": "module",
  "bin": "./dist/cli.js",
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf dist && yarn build",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": {
    "@types/node": "^20.14.0",
    "commander": "^15.0.0",
    "tsx": "^4.15.0"
  },
  "packageManager": "yarn@4.17.0",
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
